# Node 24's own build config advertises the ClangCL toolset, which isn't part of a
# default Visual Studio install. Pin the MSVC toolset so native modules
# (better-sqlite3) compile with cl.exe instead.
msvs_version=2022
clang=0
